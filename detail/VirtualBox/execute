#!/bin/bash

source detail/utils
source "$vdev_dir/vm"


#
# Main

"$vm_manage" guestcontrol "$guest_name" run --exe 'cmd.exe' --username "$user" --password "$password" --wait-stdout --wait-stderr '/E:ON' '/Q' '/C' "$@"

